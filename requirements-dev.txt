-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
httpx>=0.27.0
ruff>=0.6.0
mypy>=1.11.0

# numpy alone (not the rest of requirements-voice.txt: pywhispercpp and
# webrtcvad are heavier, platform-sensitive compiled packages not
# needed to test core/speech/ — see tests/core/speech/'s use of
# pytest.importorskip for the parts that do need them) — lets
# tests/core/speech/test_audio.py exercise pcm16_bytes_to_float32 for
# real instead of only asserting its lazy-import error path.
numpy>=1.26.0
