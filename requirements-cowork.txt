# Optional: only needed to actually run HttpCoworkTransport, the real
# core/cowork/ backend — calls the real Claude API via the official
# `anthropic` SDK. CoworkClient's retry/timeout/parsing logic imports
# nothing here and is fully tested against a fake transport.
#
#   pip install -r requirements-cowork.txt
#
# equivalently: pip install -e ".[cowork]"

-r requirements.txt

anthropic>=0.40.0
