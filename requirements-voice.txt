# Optional: only needed to actually run the speech module's real
# backends (OpenWakeWordDetector, WhisperCppSpeechToText,
# WebRtcVoiceActivityDetector, PiperTextToSpeech). Everything in
# core/speech/ imports these lazily, so the rest of Jarvis — including
# all of StreamingTranscriber's and SpeechQueue's own coordination
# logic, tested against fakes — works without this file installed.
#
#   pip install -r requirements-voice.txt
#
# equivalently: pip install -e ".[voice]"

-r requirements.txt

openwakeword>=0.6.0
pywhispercpp>=1.2.0
webrtcvad>=2.0.10
piper-tts>=1.2.0
numpy>=1.26.0
