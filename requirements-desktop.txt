# Optional: only needed for the real desktop automation/clipboard/
# screenshot/window backends (core/execution/desktop.py, clipboard.py,
# screenshot.py, window.py). filesystem/terminal/application handlers
# are dependency-free and work without this file installed.
#
#   pip install -r requirements-desktop.txt
#
# equivalently: pip install -e ".[desktop]"

-r requirements.txt

pyautogui>=0.9.54
pyperclip>=1.9.0
Pillow>=10.4.0
pygetwindow>=0.0.9; sys_platform == "win32"
